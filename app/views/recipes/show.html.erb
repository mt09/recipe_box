<div class="main_content">
	<div class="row" id="recipe_top">
		<div class="col-md-4">
			<%= image_tag @recipe.image.url(:medium), class: "recipe_box" %>
		</div>
		
		<div class="col-md-8">
			<div id="recipe_info">
				<h1><%= @recipe.title %></h1>
				<p><%= @recipe.description %></p>
				<p>Submitted by <%= @recipe.user.email %></p>
			</div>
		</div>
	</div>

	<div class="row">
		<div class="col-md-12">
			<%= link_to "Back", recipes_path, class: "btn btn-default" %>
			<%= link_to "Edit", edit_recipe_path(@recipe), class: "btn btn-default" %>
			<%= link_to "Remove", recipe_path(@recipe), method: :delete, data: {confirm: "Sure?"}, class: "btn btn-default" %>
		</div>
	</div>
</div>